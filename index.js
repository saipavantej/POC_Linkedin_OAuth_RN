/**
 * @format
 */

import {AppRegistry} from 'react-native';
import LinkedInAuth from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => LinkedInAuth);
